"""Example placeholder for where freezer-related API would go

See `project/api/site.py` for an example. This isn't implemented because it isn't needed
for the code example and would be very similar to `site.py`.
"""
